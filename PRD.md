# 文学与内心冲突｜产品需求文档（PRD）

日期：2026-09-20

任务归属：Day 4｜第 1 周

状态：AI 文档自检与复查完成，本轮未发现仍待解决的「必须修改」级问题；修订内容待用户核对。产品功能尚未开发、尚未实测。

已确认：MVP 无需登录；日精进、已采纳的分析、收藏和备注保存在当前设备的浏览器中；收藏和备注都保留。

依据：[Day 3 研究文档](research.md)及用户本次明确的 MVP 范围。本文描述产品行为，不涉及技术选型或代码。

## 1. 产品概述

将日精进总结与文学、哲学和人文社科作品联系起来，帮助用户留下自己的想法和行动，回看个人经历，并发现能够“映照当下”或帮助“探索改变”的阅读材料。

文学包括小说、诗词歌赋、散文、戏剧等；阅读方向允许交叉标注。推荐理由应对应作品实际内容，如文学的经历与情感、哲学的问题与论证、人文社科的分析与背景。

本期主要过程：**从首页进入 → 提交本次日精进并查看分析 → 确认当前处境和改变方向 → 查看两类推荐与作品详情 → 保存个人阅读兴趣或想法。**

### 1.1 本次范围调整

| 事项 | Day 4 本次要求 |
| --- | --- |
| 登录 | 移到后续，MVP 的核心操作不要求注册或登录 |
| 保存方式 | 用户确认当前浏览器保存；同一网站、同一浏览器配置刷新或重新打开后可读取，换设备不共享 |
| 推荐 | 保留当前处境和改变方向对应的基础标签匹配及 AI 讲解；长期画像、训练模型等复杂推荐移到后续 |
| 付费 | 移到后续，MVP 不设置支付入口或付费操作门槛 |
| 收藏或备注 | 用户确认两者都保留；可以独立收藏、取消收藏、增改删备注 |
| AI 分析 | 延续已确认的简短总结与反思问题雏形，原文与生成内容区分 |

与 research.md 不一致的本期范围，以用户本次要求和本 PRD 中明确记录的选择为准；research.md 保留为研究历史。

## 2. 目标用户

| 用户 | 使用场景 | 希望得到的可见结果 |
| --- | --- | --- |
| 学生 | 记录学习、成长、人际经历及实际行动 | 能再次找到自己的记录，并看到相关作品及推荐理由 |
| 部分职场人士 | 整理工作和生活中的选择与困惑 | 能用短文字完成记录，并得到简短分析、反思问题和阅读线索 |
| 文学、哲学和人文社科阅读爱好者 | 将作品的情感、问题和观点与生活联系起来 | 能查看作品详情，保存感兴趣的作品或个人阅读想法 |

用户需求仍需在后续实际试用中验证；本 PRD 不将作品推荐描述为已证实的心理改善效果。

## 3. MVP 功能

以下以“用户动作”和“可观察结果”定义功能。具体交互规则见第 5 节，字段约束见第 6 节；验收项与功能编号对应。

| 编号 | 功能 | 用户动作 | 可观察结果 |
| --- | --- | --- | --- |
| F01 | 首页两个主入口 | 点击“自总结投放分析”或“书单推荐讲解” | 分别进入日精进分析区域或书单区域；入口名称清楚，均不要求登录 |
| F02 | 提交日精进 | 输入或粘贴本次文字，选择记录日期，点击保存 | 成功后显示“已保存在此浏览器”，历史列表出现一条记录；刷新后日期与原文仍在；失败时保留输入 |
| F03 | 简短分析与反思 | 保存记录后主动点击“分析本次总结”，核对或编辑草稿，再点击“采纳并保存” | 先显示等待状态，再显示 AI 短总结和反思问题；只有采纳保存成功才成为持久记录，原文不被覆盖 |
| F04 | 确认处境与方向 | 选择当前处境和想探索的改变方向 | 页面显示本次已选条件，后续推荐基于这些条件；用户可以重新选择 |
| F05 | 书库浏览、两类荐书与讲解 | 浏览或按类型筛选书库；确认处境和方向后点击推荐，再主动请求某本作品的 AI 讲解 | 基础书目可直接浏览；两组按第 5.3 节规则给出库内结果和匹配理由；AI 讲解另行标注，不影响基础结果可用 |
| F06 | 作品详情 | 点击作品卡片的“查看详情” | 看到所选作品的名称、作者、类型、简介、资料来源及适用的推荐讲解；返回时恢复原视图及条件，展示最新列表状态 |
| F07 | 收藏与取消收藏 | 在作品详情点击“收藏”，再点击“取消收藏” | 保存成功后状态相应变化，“我的收藏”同步增减；刷新后保持最后成功状态；同一作品不会重复收藏 |
| F08 | 记录回看、编辑与删除 | 在分析区域按日期打开记录，修改并保存，或确认删除 | 打开同一条原文及已采纳分析；编辑成功更新原记录；删除成功后列表移除，刷新后保持结果 |
| F09 | 备注的新增、修改与删除 | 在作品详情输入备注并保存，修改后再次保存，或点击删除备注并确认 | 显示最后成功保存的备注及更新时间；刷新后仍在；删除后为空；每件作品保留一条可编辑备注 |
| F10 | 找回收藏和备注 | 在书单区域切换“我的收藏”或“有备注” | 只显示符合该条件的作品，能进入详情；没有结果时显示明确空状态和返回全部书目的入口 |

## 4. 后续功能

这些功能不作为 MVP 验收条件，未来启动前仍需补齐详细规则。

| 功能 | 后续用户动作 | 预期可观察结果 | 本期延后的原因 |
| --- | --- | --- | --- |
| 登录与账号管理 | 注册、登录、退出并找回账号 | 能进入和退出自己的账号；账号找回成功或失败有明确提示 | 增加账号流程与个人资料访问规则，先完成核心记录和荐书过程 |
| 账号下的云端同步 | 在另一设备登录同一账号并打开记录 | 读取该账号已保存的记录和阅读状态，不出现其他账号资料 | 用户已选择 MVP 当前浏览器保存；后续另行确定账号隔离和本地数据迁移方式 |
| 复杂推荐 | 允许使用长期记录、阅读反馈等资料生成个性化推荐 | 页面说明所用信息，并显示能解释的个性化结果；效果需验证 | 增加画像、反馈和效果评估工作，本期只处理当前明确选择 |
| 付费与会员 | 查看付费权益并购买或取消 | 价格和权益清晰，支付与取消结果可查 | 增加订单、支付和售后流程，尚无验证后的付费需求 |
| 多轮 AI 对话、长篇报告 | 围绕记录连续提问或请求长篇回顾 | 能查看连续对话或报告及其依据 | 本期 AI 聚焦短总结、反思问题与荐书讲解 |
| 外部书籍平台接入 | 从外部资料源检索并查看作品 | 显示真实资料、来源和查询失败状态 | 本期先核对人工书库；外部可用性未验证 |
| 整本作品在线阅读 | 打开作品正文并继续阅读 | 显示获准提供的正文及阅读位置 | 本期提供作品资料和推荐，不建设全文阅读器 |
| 社区与互动 | 发布内容、评论或私信 | 内容可见范围及发送结果清楚 | 先验证个人记录与阅读过程 |
| 排行榜、成就、复杂图表和原生应用 | 查看统计或在专用应用中使用 | 获得相应展示或设备体验 | 增加额外界面与维护工作，不影响核心过程 |

## 5. 页面与交互要求

### 5.1 首页

- 展示项目名称“文学与内心冲突”及一句用途说明。
- 提供“自总结投放分析”和“书单推荐讲解”两个明确可点击的主入口。
- 从任一主入口进入后，页面标题与入口含义对应；提供返回首页的路径。
- 基础流程中不出现登录或付费拦截。

### 5.2 自总结投放分析与记录回看

**输入与保存**

- 本期“投放”指输入或粘贴文字，不包含文件、图片或音频导入。
- 记录日期默认为设备当天日期，用户可改为有效的过去日期；原文为去除首尾空白后 1–5000 个字符，纯空白无效。长度包含标点与内部空格，页面显示计数；超出时提示而不静默截断。
- 每次新建产生一个记录标识，同一天允许多条；修改已有记录不新增一条。保存中按钮不可重复触发，成功后显示保存结果并进入该记录的查看状态。
- 未保存输入或分析草稿在站内切换页面或记录前提示：“继续编辑”保留内容，“放弃更改”才离开。浏览器刷新或关闭时的拦截提示受浏览器限制，未保存草稿不承诺刷新后恢复。

**分析与采纳**

- “分析本次总结”仅针对当前已保存且无未保存原文修改的记录。若尚未保存，提示先保存，不发送旧版文字。
- 用户主动请求前可见说明：本次原文将用于 AI 分析；默认不带上全部历史记录、收藏或备注。
- 生成时显示等待状态并禁止重复发起；超过 30 秒未完成视为本次超时，显示可重试状态。失效请求的迟到结果不能覆盖用户当前记录或较新的结果。
- 合格输出包含一段 1–300 个字符的简短总结及 1–2 个反思问题，每题 1–100 个字符；结果明确标为“AI 生成，供你核对”，不做心理诊断或改善评分。内容少时说明依据有限，不补造事件。字段长度与计数规则见第 6 节，内容质量按第 8 节的人工样本核对。
- 用户可编辑总结和问题后选择“采纳并保存”。编辑后仍须满足上述非空、长度及问题数量约束；校验失败时指出具体字段并保留草稿，不覆盖已保存分析。
- 采纳写入期间不能重复提交；写入成功后才显示“已采纳并保存”，刷新后保留该内容。写入失败保留本次草稿与此前已保存分析，提示重试；首次采纳失败时不能伪装成已有分析。放弃草稿不影响原文及此前已保存的分析。
- 有尚未采纳或已经编辑的分析草稿时，再次请求生成须确认是否放弃这份草稿；取消则保留草稿，确认后才开始新请求，已有持久保存的分析仍保留。
- 改变原文并保存前提示该动作会清除本条旧分析；保存成功后清除该记录的 AI 草稿和已采纳分析，提示可重新分析。仅修改日期不会清除分析。保存失败不清除原有内容。

**回看、修改与删除**

- 历史列表放在分析区域，可按记录日期从新到旧查看；同一天按创建时间从新到旧。每项展示日期、原文片段及有无已采纳分析。
- 点击记录进入其详情区域，能查看原文及已采纳分析，进入编辑或发起新的分析；有“从这条记录去选书”入口，进入书单后仍由用户选择并确认处境和方向。
- 删除记录需要确认；取消时原记录不变，成功删除后其原文与关联分析从本浏览器记录中移除。收藏和作品备注不受该操作影响。

### 5.3 书单推荐讲解

**书库与基础浏览**

- 沿用研究中的约 100 本书籍或作品集规模目标，覆盖文学、哲学、人文社科，文学覆盖诗词歌赋等形式。类别可交叉，首批书目及比例仍需内容整理时确定，不在今天实际建库。
- 功能验收使用有出处的人工书目，至少覆盖三大阅读方向并包含诗词类作品集；100 本是内容准备目标，不能将尚未整理的条目当作已存在数据。
- MVP 先按书籍或作品集展示，推荐理由可以指出有出处的篇目；单篇诗词独立建条目及计数方式留作内容范围的后续确认。
- 用户直接进入时显示“全部书目”，按人工展示顺序排列；作品类型可选“全部／文学／哲学／人文社科”。多类型作品命中任一所选类型即可显示。
- 列表提供“全部书目／我的收藏／有备注”三个视图，类型筛选与所选视图同时生效；个人视图用于找回作品，不将收藏或备注默认作为 AI 输入。

**基础推荐规则**

- “当前处境”和“改变方向”各选择一项，选项来自人工书库已有标签。处境标签和改变方向标签分开显示；标签是用户本次选择，不是系统对用户的诊断。
- 点击“查看两类推荐”后，在全部已核对书库中应用作品类型筛选，再分别匹配：处境标签相同的进入“映照当下”，改变方向标签相同的进入“探索改变”。该动作切换到推荐结果视图，不受“我的收藏／有备注”筛选限制。
- 每组按人工展示顺序排列，最多展示 3 件作品；组内不重复。同一作品允许出现在两组，但分别说明匹配的是处境还是方向。
- 每项基础理由展示“本次选择＋作品对应主题或问题”。没有任何标签命中时显示空结果，不把热门或随机作品伪装成匹配结果。
- 用户更改选择后，清除旧推荐及讲解并提示重新查看；先前结果不得继续以新条件的名义展示。

**AI 讲解**

- 用户可主动点击某项结果的“生成阅读讲解”。仅使用这项作品的已核对资料及用户确认的本次条件，不自动发送日精进原文或历史记录。
- 讲解正文与一个阅读问题合计 1–300 个字符，说明作品与本次选中组别及条件的相关之处；与已有基础理由及作品来源分开展示。AI 讲解不能替换成其他作品或虚构引语。
- 生成中的按钮不可重复触发；失败、超时、缺少必要内容或返回作品标识与本次请求不一致时，不显示为有效讲解，保留作品资料和基础理由，提供重试。超时按 30 秒处理。与作品事实和本次条件的语义一致性另按第 8 节人工样本核对，不能把字段核对等同于已识别所有内容错误。
- 阅读讲解没有“采纳并保存”动作；该动作只属于日精进分析。推荐条件、结果和阅读讲解仅在本次浏览期间临时保留，刷新后允许回到未选择状态；收藏和备注仍按已确认规则持久保存。
- 从详情返回书单时保留原浏览视图、类型筛选、推荐条件及位置，但个人列表按最新成功保存的收藏与备注重新计算。原位置超出新列表长度时显示剩余列表末尾；列表为空时显示空状态，不为保留旧结果而展示已移除项。

### 5.4 作品详情、收藏与备注

- 点击卡片后，详情名称及作者与所点作品一致。展示作品类型、简介、主题或问题、资料来源；来自推荐结果时带上所点组别和条件，展示该组匹配理由及已有讲解。同一本作品位于两组时，不能把另一组的理由冒充本次理由。
- 直接打开详情且没有推荐上下文时只展示作品资料，不推断用户处境；可返回书单选择条件后再生成阅读讲解。
- 点击“收藏”并写入成功后变为“已收藏”，再次查看可执行“取消收藏”；失败时保留之前状态。“我的收藏”与详情状态同步，同一作品不产生重复条目。
- 备注每件作品一条，去除首尾空白后 1–1000 个字符，可编辑后保存。保存成功显示内容及更新时间；空白或超长不保存并给出提示。
- 删除备注使用独立的“删除备注”动作并确认，取消时保留原文，删除成功后输入区为空、“有备注”视图中移除该项。
- 收藏和备注独立：取消收藏不会删备注，删除备注不会取消收藏。未收藏的作品也可保存备注。
- 备注存在未保存修改时，离开前可选择继续编辑或放弃；保存失败保留输入并允许重试，不覆盖此前已保存的备注。

### 5.5 当前浏览器保存与通用交互

- 保存成功的原文、日期、已采纳分析、收藏和备注，在同一网站地址、同一设备及同一浏览器配置下，刷新、关页重开后可读取。
- 页面说明“数据仅保存在当前浏览器；换设备不共享，清理浏览器数据后可能丢失”。MVP 没有账号隔离；同一浏览器配置下的使用者共享这份本地数据。
- 当前浏览器保存与 AI 处理区分：数据不进行账号云端同步，但用户主动分析或请求讲解时会提交前述必要内容用于处理；具体服务及资料处理方式在后续技术选型时核实。
- 离线时，已能打开的页面可查看本地记录与阅读状态；不承诺首次断网也能载入网站。AI 操作提示需要联网，输入和已保存内容不因此清除。
- 本地读取或写入不可用时给出明确提示，不把读取失败显示成“你还没有记录”，不显示未发生的保存成功，也不自动清空既有数据。
- AI 结果只属于发起时的记录标识与原文版本，或作品标识、组别与推荐条件。切换到其他记录、作品或推荐组别，开始修改原文、删除记录或改变推荐条件后，旧请求作废；超时、被放弃或被新请求替换的请求同样作废。迟到结果不得复活已删记录、覆盖编辑内容或贴到其他作品，回到原处后可以重新请求。
- 在手机窄屏和桌面上，主要按钮、输入区和正文可见可用；提示贴近相关操作，状态变化用文字说明，不能只依靠颜色。

## 6. 数据字段

这里描述产品需要的信息，不规定数据库、接口或存储技术。

| 数据对象 | 字段及约束 | 保存范围与用途 |
| --- | --- | --- |
| 日精进记录 | 必填：唯一标识、有效记录日期、1–5000 字符原文、原文版本标记、创建时间、更新时间；同一天可有多个标识 | 当前浏览器持久保存；编辑保留标识，原文更改更新版本；不含账号字段 |
| 分析内容 | 记录标识、依据的原文版本、1–300 字符总结、1–2 个各为 1–100 字符的问题、草稿／已采纳状态；采纳时间仅在写入成功后记录 | AI 草稿临时保留，用户采纳成功后持久保存；编辑后的草稿同样校验，保存失败不覆盖旧值；输入版本改变时按第 5.2 节清除 |
| 本次推荐条件 | 当前处境标签、改变方向标签、作品类型（全部或三大类之一）；标签必须属于当前书库选项 | 本次浏览期间使用；生成两类推荐前前两项均必填 |
| 书库作品 | 必填：唯一且稳定的标识、名称、作者或来源可核对的署名、至少一个作品类型、简介、主题或问题、资料来源名称及链接、唯一展示顺序；处境标签与方向标签可为空 | 人工核对的公共资料；无对应标签的作品可浏览但不进入该组匹配；署名不明必须如实说明 |
| 推荐结果 | 作品标识、组别、所依据的条件、对应标签、基础理由、AI 讲解状态、讲解正文及一个阅读问题（合计 1–300 字符） | 临时结果，无采纳保存状态；依赖的条件改变后清除；详情沿用被点击项的组别与条件 |
| 本次生成状态 | 本次生成标识、所属记录与原文版本或作品与组别及条件、生成中／成功／失败／已作废状态 | 仅临时使用；核对返回结果的归属，避免迟到结果覆盖新状态，不属于长期用户画像 |
| 收藏 | 作品标识、收藏时间；每件作品最多一条有效收藏 | 当前浏览器持久保存；取消收藏只移除收藏关系，不影响备注 |
| 备注 | 作品标识、1–1000 字符正文、创建时间、更新时间；每件作品最多一条 | 当前浏览器持久保存；保存覆盖该作品旧备注，删除备注不影响收藏 |

字符限制采用页面显示的同一计数规则，包含标点和内部空格；校验空值前先去除首尾空白。暂不建立账号、支付或长期用户画像字段。书籍与个人数据通过稳定作品标识关联，不只靠可能重名的标题。

## 7. 异常状态

| 情况 | 用户动作 | 页面应显示的结果 |
| --- | --- | --- |
| 无记录、收藏或备注 | 首次打开对应区域 | 显示对应空状态及新建记录或返回书库入口，不伪造示例为用户数据 |
| 原文或备注为空、超长，日期无效 | 点击保存 | 指出具体问题，不创建或覆盖数据；输入仍保留 |
| 记录、已编辑分析或备注保存失败 | 点击保存或采纳并保存 | 显示未保存并允许重试，保留当前输入或草稿及之前成功保存的数据；首次分析采纳失败不产生已采纳记录 |
| 已编辑分析为空、超长或问题数量不符 | 点击采纳并保存 | 指出具体字段与限制，保留草稿，不覆盖原文或此前已保存分析 |
| AI 等待、断网、超时或失败 | 请求分析或讲解 | 显示相应状态；超时阈值为 30 秒，恢复后可重试；原文和基本书目仍可使用 |
| AI 输出字段不全、过长或作品标识不对应 | 查看生成结果 | 不作为结构完整的成功结果，显示生成失败并允许重试；无效讲解不替换原作品资料。事实和语义质量另由人工样本核对 |
| 生成期间切换对象、编辑或删除原文、改变推荐条件 | 执行对应操作 | 旧请求作废；迟到结果不得覆盖新状态或重建已删除内容，用户可以重新请求 |
| 推荐条件未选全 | 请求两类推荐 | 指出缺少的条件，不以未经确认的猜测替代用户选择 |
| 某组没有匹配作品 | 查看推荐结果 | 该组显示暂无匹配并允许调整条件，其他有结果的组正常展示 |
| 类型与个人视图的组合无结果 | 切换筛选或视图 | 显示无符合条件作品，提供清除筛选或返回全部书目的操作 |
| 作品标识无效或必要资料缺失 | 打开对应详情 | 显示作品暂不可用及返回入口，不用另一作品冒充，不因此清除个人数据 |
| 收藏或取消收藏写入失败 | 点击对应按钮 | 保留此前成功状态并提示失败；列表与详情不出现相互矛盾的成功状态 |
| 删除记录或备注失败 | 确认删除 | 不从已保存列表中伪装移除，提示失败并允许重试 |
| 存储不可读或不可写 | 打开或保存个人内容 | 区分读取失败与空数据，区分写入失败与成功；不自动清除旧数据 |
| 连续重复点击 | 保存或生成过程中再次点击 | 本次处理中按钮不可重复触发，不产生重复记录、收藏或并行生成结果 |

## 8. 文学与内心冲突｜逐条验收标准

本节用于后续逐项检查产品行为。当前只是在编写需求，所有功能均尚未开发、尚未实测。

验收准备：使用虚构日精进和测试用个人数据；书库需有可核对的来源、三大类型及诗词类作品集，并具备稳定标识、唯一展示顺序和明确的处境／方向标签。准备某组至少 4 个匹配项以验证最多展示 3 项；准备同一作品两组均命中的情况；通过作品类型筛选，使全库中存在的某个标签在筛选结果中无匹配，用于单组或双组空结果验证。浏览器与 AI 异常应在可控验证环境中模拟，不操作真实用户资料。

AI 内容人工核对方法：为虚构日精进预先列出已给出的事件、行动和不确定信息，逐句比对总结，发现新增个人事实、诊断或评分即该用例不通过；为文学、哲学、人文社科作品分别准备来源与本次选择，核对讲解有无资料支持、是否回答本组匹配理由、是否编造引语。短输入也需列入样本。以上检查与字段长度、数量、作品标识等可直接判断的结构检查分别记录；有限样本通过不代表今后每次 AI 输出都正确。

### A. 入口、记录与分析

- [ ] AC01（F01）：打开首页，看到项目名称和两个指定入口；分别点击进入对应区域，均能返回首页，全程无需登录或付费。
- [ ] AC02（F02）：输入有效日期和一段虚构日精进，点击保存，列表只新增一条；刷新、关闭页面后在同一浏览器重新打开，日期与原文保持一致。
- [ ] AC03（F02）：依次输入纯空白、5001 个字符、无效日期和未来日期，保存均被阻止且原因明确；已有记录不被覆盖。1 个有效字符和 5000 个字符可通过长度校验。
- [ ] AC04（F02/F03）：保存或生成过程中重复点击，不产生重复记录或同时生成的多份结果；保存失败时输入保留且未显示成功。
- [ ] AC05（F03）：仅输入尚未保存的文字或修改原文未保存时，请求分析提示先保存；保存后用户主动请求才生成，操作前可看到本次文字会用于 AI 分析的说明。
- [ ] AC06（F03）：有效分析结果明确标为 AI 生成，包含 1–300 字符的短总结和 1–2 个各为 1–100 字符的问题；原文仍可查看且未被覆盖。
- [ ] AC07（F03）：用含明确事件与行动的虚构日精进核对分析，没有新增未提供的个人事实、心理诊断或改善评分；资料不足时明确表达限制。
- [ ] AC08（F03）：编辑分析后点击采纳并保存，刷新后保留编辑后的结果；只生成但未采纳的草稿不被当作已保存分析，放弃草稿不删除此前已保存内容。
- [ ] AC09（F03）：模拟失败或超过 30 秒未完成，显示失败或超时及重试入口；已保存原文不丢失，无效字段或超长结果不作为成功结果。
- [ ] AC10（F08）：创建不同日期和同日多条记录，列表按规定顺序展示；打开任何一条，原文、日期和已采纳分析都属于该记录。
- [ ] AC11（F08）：修改原文并保存，只更新原记录，旧分析被清除且有提示；仅修改日期时保留分析。旧请求迟到不把结果贴到新版本或别的记录。
- [ ] AC12（F08）：删除记录时取消，记录不变；确认且删除成功后列表移除，刷新仍不出现，收藏和作品备注保持不变。

### B. 书单、推荐与详情

- [ ] AC13（F05）：从首页直接进入书单，可在未提交日精进时浏览全部书目；按文学、哲学、人文社科筛选得到对应类型，未选推荐条件时不显示个性化标签。
- [ ] AC14（F04/F05）：只选处境或只选方向后请求推荐，提示补齐；两项齐全后，页面清楚展示本次条件和“映照当下／探索改变”两组。
- [ ] AC15（F05）：对照人工标签逐项核对结果：每组只出现该组标签命中的库内作品，应用类型筛选，按展示顺序最多取 3 项，组内无重复；跨组同书分别说明原因。
- [ ] AC16（F05）：选择仅一组有匹配的条件，有结果的组正常展示，另一组明确暂无匹配；不塞入不匹配作品。改变条件后旧结果被清除并可重新生成。
- [ ] AC17（F05）：在“我的收藏／有备注”视图请求两类推荐，结果来自应用类型筛选后的全部书库，不错误地局限于个人列表。
- [ ] AC18（F05）：主动生成某项阅读讲解，正文和一个阅读问题合计 1–300 字符，按本节人工核对方法确认与该作品及本组条件相符、有资料依据且未编造引语；所用资料与生成讲解可区分。
- [ ] AC19（F05）：讲解失败、超时或作品不对应时，不将其展示为有效讲解；原书目、简介和基础匹配理由仍可查看，重试可用。
- [ ] AC20（F06）：分别从两组打开同一本作品，标识、名称、作者和类型一致，但显示的是所点组别的匹配理由与讲解；返回时保留原视图与条件，并按最新收藏、备注状态重算个人列表，位置超出时按第 5.3 节处理。
- [ ] AC21（F06）：直接打开无推荐上下文的有效作品详情，仅显示作品资料及个人阅读状态，不编造个性化理由；无效标识显示不可用与返回入口。

### C. 收藏、备注与持久保存

- [ ] AC22（F07/F10）：收藏一件作品后，详情显示已收藏，“我的收藏”只出现一项；刷新或关闭重开后保持，反复操作不会增加重复条目。
- [ ] AC23（F07）：从“我的收藏”打开一件有备注的作品并取消收藏，返回后保留原视图与类型筛选，该作品已从列表移除；如果它是最后一项则显示空状态。刷新后仍未收藏，但原备注保留。
- [ ] AC24（F09/F10）：给未收藏作品保存备注，“有备注”中可找到，详情内容一致；修改并保存后刷新显示新内容，且不会自动变为收藏。
- [ ] AC25（F09）：保存空白或 1001 个字符备注被阻止并保留输入；1000 个字符可保存。模拟写入失败，旧备注不被覆盖，页面不提示成功。
- [ ] AC26（F09）：从“有备注”打开作品，删除时取消则原文不变；确认且删除成功后返回原视图，该项移除或显示空状态，刷新不再显示该备注，作品原有收藏状态不变。
- [ ] AC27（F10）：切换“我的收藏／有备注”并叠加类型筛选，只显示符合条件的作品；无结果时出现空状态及返回全部书目的入口。
- [ ] AC28（F02/F07/F08/F09）：模拟记录写入、取消收藏或删除备注失败，原状态保留且可重试；读取失败有错误提示，不冒充空数据，不自动清空既有记录。
- [ ] AC29（通用）：有未保存原文、分析草稿或备注修改时在站内切换，选择继续编辑会保留内容，选择放弃才丢弃未保存部分；此前保存内容不受影响。本用例不要求控制浏览器的刷新或关闭弹窗。
- [ ] AC30（通用）：页面明确说明当前浏览器保存及换设备不共享；在新浏览器配置中不自动出现旧配置的个人数据。生成请求不默认包含历史记录、收藏或备注。
- [ ] AC31（通用）：已打开页面断网后，已有本地内容仍可查看，AI 操作提示联网或失败；恢复网络后可重试，原输入不被清除。
- [ ] AC32（通用）：在约 375 像素宽的手机视图及桌面视图，主要按钮、输入与状态文字可见可操作，正文不被遮挡，错误提示不只靠颜色区分。

### D. 自检补充的保存与生成状态用例

- [ ] AC33（F03）：把分析总结改为空白、改为 301 个字符、将任一问题改为空白或 101 个字符，或使问题数量不在 1–2 个内，采纳均被阻止且草稿保留；恢复合法内容后模拟写入失败，旧分析仍在且没有新采纳成功提示，重试成功后刷新才看到新分析。
- [ ] AC34（F03/F05/F08）：分别在生成中切换记录、编辑原文、删除记录、切换到其他作品、在同一作品的两组推荐间切换、改变推荐条件，并模拟旧请求迟到；结果均不贴到新对象或另一组、不复活已删除记录。超时后重试成功，再收到旧请求结果，也不能覆盖新的结果。
- [ ] AC35（F03/F05）：存在未采纳的分析草稿时再次生成，取消确认会保留草稿，确认才开始新请求；阅读讲解没有“采纳并保存”按钮，刷新后可以清空，但已保存的日精进分析、收藏和备注仍在。

## 9. 文档状态与后续核对

- 已收到用户选择：当前设备浏览器保存；收藏与备注都保留。相关功能、数据和验收条目已补齐。
- 本稿的输入长度、输出长度、超时和每组展示数量是为便于验收而拟定的产品边界，随本稿一并交用户核对，不代表已选定具体技术服务。
- 约 100 本是沿用的内容规模目标；实际书目和类别比例在内容准备时核实，功能验收样本要求见第 8 节。单篇诗词独立条目的范围暂不纳入本稿。
- 登录、复杂推荐、付费及账号云端同步已列为后续。今日不选框架、数据库、模型或部署服务。
- 已执行 AI 文档自检、必要修订和复查，结果见第 10 节。第 8 节验收项仍全部未勾选，等待功能开发后实际验证。

## 10. AI 自检记录｜2026-09-20

### 10.1 检查方法与分级

- 对照用户最新范围及已确认的两个选择，逐项核对八个必需部分、F01–F10、字段、异常状态和验收条目。
- 检查每项功能能否从“用户动作”推出明确结果，正常与失败状态是否一致，保存、返回、切换对象时是否会出现相互矛盾的要求。
- 「必须修改」指会导致核心动作结果不确定、功能遗漏或前后要求冲突的问题；措辞和验收方法的澄清另行记录。
- 自检对象是 PRD 文档；采用文本阅读、功能与验收条目对照、按描述推演操作结果。没有运行网站、调用模型或执行产品用例。

### 10.2 首轮问题与修订复查

| 编号 | 初检级别 | 发现的问题 | 实际修订 | 复查依据与结果 |
| --- | --- | --- | --- | --- |
| R1 | 必须修改 | 分析可编辑并采纳，但编辑后的输入限制、写入失败和旧分析保留规则未写全，无法确定失败后应显示什么 | 补齐编辑后非空、长度、问题数量校验，草稿保留、旧值保留、成功才标记采纳及重新生成前的确认 | 第 5.2、6、7 节与 AC08、AC33、AC35 一致；失败不覆盖旧值，重试成功才显示新值。已解决 |
| R2 | 必须修改 | “返回时保留原书单结果”与取消收藏、删除备注后个人列表立即更新可能冲突 | 明确保留的是视图和条件，个人列表按最新成功保存状态重算；补齐最后一项移除的空状态和位置规则 | 第 5.3、5.4 节与 AC20、AC23、AC26 一致；取消收藏或删除备注后返回不会显示过时条目。已解决 |
| R3 | 必须修改 | “未采纳的讲解”暗含未定义的讲解保存功能；同书两组及迟到结果的归属还不够完整 | 明确只有日精进分析可采纳，阅读讲解仅临时保留；补齐记录版本、作品和组别归属及请求失效规则 | 第 5.3、5.5、6、7 节与 AC20、AC34、AC35 一致；没有新增讲解保存动作，也不会用旧结果覆盖另一对象或组别。已解决 |

同时完成的验收表述澄清：统一 AI 输出长度的字符口径；补齐空结果、超过三项及同书跨组的样本条件；区分结构检查和人工内容核对；将未保存内容的离开确认限定为站内操作。这些修改细化已有功能的行为，没有加入登录、付费或复杂推荐。

### 10.3 最终范围与覆盖核对

| 检查项 | 文档证据 | 本轮结果 |
| --- | --- | --- |
| 八个必需部分齐全 | 第 1–8 节 | 通过文档检查 |
| 首页两个入口及无需登录 | F01、第 5.1 节、AC01 | 通过文档检查 |
| 日精进写入、分析和回看 | F02、F03、F08；AC02–AC12、AC33–AC35 | 通过文档检查 |
| 两类推荐、作品详情及讲解 | F04–F06；AC13–AC21、AC34–AC35 | 通过文档检查 |
| 收藏与备注各自增改及回看 | F07、F09、F10；AC22–AC28 | 通过文档检查 |
| 当前浏览器保存及异常状态 | 第 5.5、6、7 节；AC28–AC35 | 通过文档检查 |
| 登录、复杂推荐、付费列为后续 | 第 1.1、4、9 节 | 通过文档检查 |
| 可逐项核对，且不伪装为已经运行 | AC01–AC35 均未勾选；本节注明自检方式 | 通过文档检查 |

**复查结论：首轮 3 项「必须修改」均已处理；本轮复查未发现仍待解决的「必须修改」级问题，剩余 0 项。**

本结论只说明当前 PRD 在本次范围下的完整性、一致性和可核对性。书目内容、用户真实反馈、技术可行性和 AI 实际输出质量仍需后续准备或验证，不因文档自检而视为已完成。PRD 尚待用户核对修订及确认提交。
